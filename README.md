# Cars24 SDUI Assignment

> Status: in progress. This README is being filled in incrementally as the
> system is built — see git history for the actual build order.

## 1. Screen chosen

TODO: which Cars24 screen (home/landing recommended), and why it clears the
complexity bar (5+ section types, a rail, a grid/list, an interactive SDUI
action).

## 2. Setup

TODO: how to open/build/run (Android Studio version, JDK, minSdk/target).

## 3. Architecture overview

TODO: node model, component registry, action dispatcher, state model.
Diagram or short walkthrough of `sdui/` package layout.

## 4. Schema design rationale

TODO: why the schema looks the way it does, what it optimizes for
(forward-compatibility / unknown-type fallback / action extensibility).

## 5. Versioning story

TODO: how an old app version coexists with a newer server payload
(e.g. `minSupportedVersion` field, unknown-type fallback doubling as the
forward-compat mechanism, additive-only prop changes).

## 6. Trade-offs / what was cut

TODO: honest list of what was scoped out given the 72h timebox, and why.

## Related docs

- [PERF.md](PERF.md) — performance methodology and results
- [COVERAGE.md](COVERAGE.md) — schema coverage claim
- [AI_WORKFLOW.md](AI_WORKFLOW.md) — AI collaboration evidence
